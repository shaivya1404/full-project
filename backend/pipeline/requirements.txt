# Core
websockets>=12.0
numpy>=1.24.0
aiohttp>=3.9.0

# Speech & Audio
openai-whisper>=20231117
torch>=2.1.0
torchaudio>=2.1.0

# Silero-VAD (loaded via torch.hub)
# No pip install needed — torch.hub.load handles it

# Emotion recognition
modelscope>=1.9.0
# emotion2vec+ model is downloaded automatically by modelscope on first run:
#   iic/emotion2vec_plus_large

# Optional: scipy for higher-quality resampling
# scipy>=1.11.0
